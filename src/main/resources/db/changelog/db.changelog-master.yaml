databaseChangeLog:
    - include:
         file: db/changelog/changeset/create-passengers.yaml


    #    id bigint NOT NULL GENERATED BY DEFAULT AS IDENTITY ( INCREMENT 1 START 1 MINVALUE 1 MAXVALUE 9223372036854775807 CACHE 1 ),
    # age integer NOT NULL,
    #count_parents_or_children_on_board integer,
    #count_siblings_or_spouses_on_board integer,
    #fare double precision NOT NULL,
    #grade character varying(255) COLLATE pg_catalog."default",
    #name character varying(100) COLLATE pg_catalog."default" NOT NULL,
    #sex character varying(255) COLLATE pg_catalog."default" NOT NULL,
    # survived boolean,
    # CONSTRAINT passengers_pkey PRIMARY KEY (id),
    #CONSTRAINT passengers_age_check CHECK (age >= 0),
    #CONSTRAINT passengers_count_parents_or_children_on_board_check CHECK (count_parents_or_children_on_board >= 0),
    #CONSTRAINT passengers_count_siblings_or_spouses_on_board_check CHECK (count_siblings_or_spouses_on_board >= 0),
    #CONSTRAINT passengers_fare_check CHECK (fare >= 0::double precision),
    #CONSTRAINT passengers_grade_check
